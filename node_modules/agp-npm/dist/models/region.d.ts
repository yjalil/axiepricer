/** Represents the region origin of a given Axie. A region can either be Global or Japan. */
export declare enum Region {
    Global = "global",
    Japan = "japan"
}
export declare const binRegionMap: Map<string, Region>;
